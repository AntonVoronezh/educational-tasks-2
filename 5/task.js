import cube from './numbers1';//x
import multi, { pow2 as pow, sub2 as sub, sum2 as sum } from './numbers2';
import sqrt from './numbers3';//x

export { pow, sum, sub, sqrt, multi, cube as default };
